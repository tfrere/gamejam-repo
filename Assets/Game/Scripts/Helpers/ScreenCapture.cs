
using UnityEngine;
using UnityEditor;
using System.Collections;
using System.IO;

public class ScreenCapture : MonoBehaviour
{
    public void Awake()
    {
        // int superSize = 3;
        // string filepath = EditorApplication.currentScene;
        // string levelname = Application.loadedLevelName;
        // filepath = filepath.Remove (filepath.Length-11,11);
        // string name = "screenshot.png";
        // string filename = filepath + levelname + name;
        // Debug.Log(filename);
        // Application.CaptureScreenshot(filename,superSize);
        // Debug.Log ("screenshot taken");

    }
}
